<view class="page bms-page">
  <view class="topbar">
    <view>
      <view class="eyebrow">BMS</view>
      <view class="title">选择电池</view>
    </view>
    <view class="status-dot {{state.bmsConnected ? 'online' : ''}}"></view>
  </view>

  <view wx:if="{{!state.dashboardConnected}}" class="connect-card">
    <view class="connect-title">先连接X仪表</view>
    <view class="connect-text">连接后即可扫描并选择要使用的 BMS。</view>
    <button class="btn primary" loading="{{busy}}" disabled="{{busy}}" bindtap="connectInstrument">连接X仪表</button>
  </view>

  <view wx:else>
    <view class="selected-panel">
      <view>
        <view class="selected-label">当前选择</view>
        <view class="selected-name">{{selectedName || '未选择BMS'}}</view>
        <view wx:if="{{selectedMac}}" class="selected-mac">{{selectedMac}}</view>
      </view>
      <view class="selected-state {{state.bmsConnected ? 'online' : ''}}">
        {{state.bmsConnected ? '已连接' : '未连接'}}
      </view>
    </view>

    <button class="btn primary main-btn" loading="{{busy}}" disabled="{{busy || !selectedMac}}" bindtap="connectSelectedBms">连接此BMS</button>
    <button class="btn secondary main-btn" loading="{{state.bmsScanning}}" disabled="{{busy || state.bmsScanning}}" bindtap="scanBms">扫描附近BMS</button>

    <view class="section">
      <view class="section-title">历史连接</view>
      <view wx:if="{{bmsHistory.length === 0}}" class="empty">连接成功后，历史 BMS 会显示在这里。</view>
      <view wx:for="{{bmsHistory}}" wx:key="mac" class="bms-card {{item.selected ? 'selected' : ''}}" bindtap="chooseBms" data-mac="{{item.mac}}">
        <view class="card-head">
          <view class="bms-name">{{item.name || '未命名BMS'}}</view>
          <view wx:if="{{item.isLast}}" class="tag">上次连接</view>
        </view>
        <view class="signal">信号 {{item.signalText}}</view>
        <view class="mac">{{item.mac}}</view>
      </view>
    </view>

    <view class="section">
      <view class="section-title">附近 BMS</view>
      <view wx:if="{{bmsList.length === 0}}" class="empty">点击“扫描附近BMS”查找可连接的电池。</view>
      <view wx:for="{{bmsList}}" wx:key="mac" class="bms-card {{item.selected ? 'selected' : ''}}" bindtap="chooseBms" data-mac="{{item.mac}}">
        <view class="card-head">
          <view class="bms-name">{{item.name || '未命名BMS'}}</view>
          <view wx:if="{{item.isLast}}" class="tag">上次连接</view>
        </view>
        <view class="signal">信号 {{item.signalText}}</view>
        <view class="mac">{{item.mac}}</view>
      </view>
    </view>
  </view>
</view>
